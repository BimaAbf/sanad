# Test-only overrides, layered over .env by `just migrate-test`.
# Contains no secret: it points at the local docker-compose Postgres.
MISK_DATABASE_URL=postgresql+asyncpg://misk:misk@localhost:55432/misk_test
